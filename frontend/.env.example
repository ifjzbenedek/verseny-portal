# Local dev: leave empty, Vite proxies /api to localhost:8081 (see vite.config.ts)
# Vercel production: set this to your Render backend URL, e.g.
# VITE_API_URL=https://verseny-backend.onrender.com
VITE_API_URL=
